/// --- Alertas -----
const alertasContainer = document.querySelector('#alertas-container');
if (alertasContainer) {
    setTimeout(() => {
        alertasContainer.remove();
    }, 3000);
}
/// ------------------

